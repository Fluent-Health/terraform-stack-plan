# Build output
/tfstackplan
*.out
/dist/

# Secrets — never commit
.env
*.pem
*.key
*.p12
*.pfx
service-account*.json
*-sa-key.json

# brainstorming visual companion
.superpowers/

# Superpowers brainstorming specs + implementation plans — local working
# scratch. Durable rationale lives in PR descriptions; current truth lives in
# docs/DESIGN.md. See CLAUDE.md.
docs/superpowers/
web/node_modules/
web/ui/node_modules/
web/ui/dist/

# git worktrees (isolated dev workspaces; see using-git-worktrees)
.worktrees/
